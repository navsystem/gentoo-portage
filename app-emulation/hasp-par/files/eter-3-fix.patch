diff -up a/aksparpub.c b/aksparpub.c
--- a/aksparpub.c	2009-10-12 13:35:36.000000000 +0400
+++ b/aksparpub.c	2008-10-30 10:02:52.000000000 +0300
@@ -85,6 +85,9 @@ unsigned int loglevel = 1;
 
 #if LINUX_VERSION_CODE >= 0x20600
 
+#include <linux/vermagic.h>
+MODULE_INFO(vermagic, VERMAGIC_STRING);
+
 static const char __module_depends[]
 /* __attribute_used__ - only for functions? see eterbug #2673 */
 __attribute__((section(".modinfo"))) =
@@ -519,7 +522,8 @@ int init_module(void)
         }
 
 #if LINUX_VERSION_CODE >= 0x20600
-        printk(KERN_INFO "%s: found parport %u at %04lX\n", hardlock, i, (unsigned long)port->base);
+        if (loglevel >= 3)
+            printk(KERN_INFO "%s: found parport %u at %04lX\n", hardlock, i, (unsigned long)port->base);
 #endif
 
         hl_pports[hl_numports].port = port->base;
